#include <bits/ stdc++.h>
using namespace std;
int main (){
    // freopen("dpbabyl.inp", "r", stdin);
    // freopen("dpbabyl.out", "w", stdout);
    long long n;
    while (cin >> n && n !=0){
        for (long long i =0; i < n; i++){
            long long a,b,c;
            cin >>a>>b>>c;
            
        }
    }
}